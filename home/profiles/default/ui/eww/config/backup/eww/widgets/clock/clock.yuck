(defwidget clock []
  (box
    :class "clock"
    (eventbox
      :cursor "pointer"
      :onhover "eww open moment --toggle"
      :onhoverlost "eww open moment --toggle"
      :class "button-clock"
      (box
        :class "button-clock-text"
        "${hour}:${minute}"))))
        
