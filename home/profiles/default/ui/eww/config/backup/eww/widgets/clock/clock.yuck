(defwidget clock []
  (box
    :class "clock"
    :tooltip date
    (button
      :onclick ""
      :class "button-clock"
      (box
        :class "button-clock-text"
        clock_time))))
        
